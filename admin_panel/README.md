# Admin Panel для AltShop Bot

Веб-панель управления для Telegram бота AltShop. Построена на современном стеке технологий с использованием Material UI и dark mode по умолчанию.

## 🚀 Технологии

### Backend
- **NestJS** - Node.js фреймворк
- **TypeORM** - ORM для работы с PostgreSQL
- **Passport JWT** - аутентификация
- **Swagger** - документация API

### Frontend
- **React 18** - UI библиотека
- **Vite** - сборщик
- **Material UI v5** - компоненты интерфейса
- **React Query** - управление серверным состоянием
- **Zustand** - управление клиентским состоянием
- **React Router v6** - маршрутизация

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx** - reverse proxy
- **pnpm** - менеджер пакетов
- **Turborepo** - монорепозиторий

## 📁 Структура проекта

```
admin_panel/
├── apps/
│   ├── backend/          # NestJS API
│   │   ├── src/
│   │   │   ├── entities/     # TypeORM сущности
│   │   │   ├── modules/      # Модули (auth, users, plans, etc.)
│   │   │   └── main.ts       # Точка входа
│   │   └── Dockerfile
│   └── frontend/         # React приложение
│       ├── src/
│       │   ├── api/          # API клиент
│       │   ├── components/   # Компоненты
│       │   ├── layouts/      # Layouts
│       │   ├── pages/        # Страницы
│       │   ├── stores/       # Zustand stores
│       │   └── theme/        # Material UI тема
│       ├── nginx.conf
│       └── Dockerfile
├── nginx/
│   └── nginx.conf        # Конфигурация для интеграции с altshop nginx
├── docker-compose.yml
└── package.json
```

## 🛠 Установка и запуск

### Требования
- Node.js 20+
- pnpm 8+
- Docker & Docker Compose

### Локальная разработка

1. **Установка зависимостей:**
```bash
cd admin_panel
pnpm install
```

2. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

3. **Запуск в режиме разработки:**
```bash
# Запуск всех сервисов
pnpm dev

# Или по отдельности
pnpm --filter backend dev
pnpm --filter frontend dev
```

### Docker Compose

1. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл
```

2. **Сборка и запуск:**
```bash
docker-compose up -d --build
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

## 🔧 Интеграция с существующим nginx

Для интеграции с существующим nginx бота, замените файл `altshop/nginx/nginx.conf` на `admin_panel/nginx/nginx.conf` или добавьте следующие блоки в существующую конфигурацию:

```nginx
# Добавьте upstreams
upstream admin-frontend {
    server admin-frontend:80;
}

upstream admin-backend {
    server admin-backend:3001;
}

# Добавьте locations в server блок
location /admin {
    proxy_http_version 1.1;
    proxy_pass http://admin-frontend;
    proxy_set_header Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

location /admin/api/ {
    rewrite ^/admin/api/(.*)$ /$1 break;
    proxy_http_version 1.1;
    proxy_pass http://admin-backend;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

## 🔐 Аутентификация

Панель использует JWT аутентификацию. Для создания первого администратора:

1. Подключитесь к базе данных
2. Выполните SQL запрос:
```sql
INSERT INTO admin_users (username, email, password, role, is_active)
VALUES ('admin', 'admin@example.com', '$2b$10$...hashed_password...', 'super_admin', true);
```

Или используйте API endpoint для регистрации (если включен):
```bash
curl -X POST https://bot.domain.com/admin/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "email": "admin@example.com", "password": "your_password"}'
```

## 📱 Функциональность

### Dashboard
- Общая статистика (пользователи, подписки, доход)
- Графики активности
- Последние транзакции

### Пользователи
- Список всех пользователей бота
- Детальная информация о пользователе
- История подписок и транзакций

### Тарифные планы
- Управление планами подписок
- Настройка цен и периодов

### Подписки
- Просмотр активных подписок
- Управление статусами

### Транзакции
- История платежей
- Фильтрация по статусу

### Промокоды
- Создание и управление промокодами
- Статистика использования

### Настройки
- Общие настройки бота
- Настройки платежей
- Реферальная и партнёрская программы

## 🎨 Темы

Панель поддерживает светлую и тёмную темы. По умолчанию используется тёмная тема. Переключение доступно в header панели.

## 📝 API Документация

После запуска backend, Swagger документация доступна по адресу:
```
https://bot.domain.com/admin/api/docs
```

## 🔄 Обновление

```bash
# Остановка контейнеров
docker-compose down

# Получение обновлений
git pull

# Пересборка и запуск
docker-compose up -d --build
```

## 📄 Лицензия

MIT