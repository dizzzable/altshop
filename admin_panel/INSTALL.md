# 🚀 Полное руководство по установке Admin Panel

Это пошаговое руководство для новичков. Следуйте инструкциям по порядку.

---

## 📌 Важная информация

| Параметр | Значение |
|----------|----------|
| **Домен панели** | `https://remnabot.2get.pro/admin` |
| **Новый домен нужен?** | ❌ НЕТ, используется тот же |
| **Новый SSL-сертификат?** | ❌ НЕТ, используется существующий |
| **Конфликт nginx?** | ❌ НЕТ, используется существующий nginx |
| **Отдельный запуск?** | ❌ НЕТ, запускается вместе с ботом |

---

## 📋 Содержание

1. [Требования](#требования)
2. [Подготовка сервера](#подготовка-сервера)
3. [Загрузка файлов на сервер](#загрузка-файлов-на-сервер)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Запуск](#запуск)
6. [Создание администратора](#создание-администратора)
7. [Проверка работы](#проверка-работы)
8. [Решение проблем](#решение-проблем)

---

## 📦 Требования

Перед началом убедитесь, что у вас:

- ✅ Работающий бот AltShop (уже установлен и запущен)
- ✅ Доступ к серверу по SSH
- ✅ Docker и Docker Compose установлены на сервере

### Как проверить, что Docker установлен:

```bash
# Подключитесь к серверу по SSH и выполните:
docker --version
docker-compose --version
```

Если команды работают и показывают версии - всё готово!

---

## 🖥️ Подготовка сервера

### Шаг 1: Подключитесь к серверу

Откройте терминал (на Windows можно использовать PowerShell или PuTTY) и подключитесь:

```bash
ssh root@ваш_ip_адрес
```

Введите пароль, когда попросят.

### Шаг 2: Перейдите в папку с ботом

```bash
cd /путь/к/вашему/боту
```

Обычно это что-то вроде:
```bash
cd /root/altshop
# или
cd /home/user/altshop
```

### Шаг 3: Проверьте, что бот работает

```bash
docker-compose ps
```

Вы должны увидеть запущенные контейнеры (remnashop, altshop-db, altshop-redis, altshop-nginx).

---

## 📁 Загрузка файлов на сервер

### Вариант 1: Через Git (рекомендуется)

Если admin_panel находится в том же репозитории:

```bash
# Обновите репозиторий
git pull
```

### Вариант 2: Через SFTP (FileZilla, WinSCP)

1. Откройте FileZilla или WinSCP
2. Подключитесь к серверу:
   - Хост: ваш_ip_адрес
   - Порт: 22
   - Пользователь: root (или ваш пользователь)
   - Пароль: ваш пароль
3. Загрузите папку `admin_panel` в папку с ботом

### Вариант 3: Через SCP (командная строка)

На вашем локальном компьютере:

```bash
scp -r admin_panel root@ваш_ip_адрес:/путь/к/боту/
```

---

## ⚙️ Настройка переменных окружения

> **🎉 Упрощённая настройка!** Admin Panel теперь использует тот же `.env` файл, что и бот. Вам нужно только добавить пару переменных.

### Шаг 1: Откройте .env файл бота

```bash
cd altshop
nano .env
```

### Шаг 2: Добавьте переменные для Admin Panel

В конец файла `.env` добавьте:

```env
# - - - - - ADMIN PANEL CONFIGURATION - - - - - #

# Secret key for JWT token signing in admin panel.
# !!! CRITICALLY IMPORTANT: Use a strong, unique key in production.
JWT_SECRET=change_me_to_strong_secret_key

# JWT token expiration time.
JWT_EXPIRES_IN=7d
```

### Шаг 3: Сгенерируйте JWT_SECRET

```bash
# Сгенерируйте случайный ключ
openssl rand -hex 32
```

Скопируйте результат и вставьте вместо `change_me_to_strong_secret_key`.

### Шаг 4: Сохраните файл

- Нажмите `Ctrl + X`
- Нажмите `Y` (да, сохранить)
- Нажмите `Enter`

### 📝 Таблица переменных Admin Panel

| Переменная | Описание | Пример значения |
|------------|----------|-----------------|
| JWT_SECRET | Секретный ключ для JWT токенов | a8f5f167f44f4964... |
| JWT_EXPIRES_IN | Время жизни токена | 7d |

> **Примечание:** Все остальные переменные (база данных, Redis) берутся из существующего `.env` файла бота автоматически!

---

## 🚀 Запуск

> **🎉 Всё просто!** Admin Panel теперь запускается автоматически вместе с ботом. Никаких отдельных команд!

### Шаг 1: Перейдите в папку бота

```bash
cd altshop
```

### Шаг 2: Пересоберите и запустите всё

```bash
docker-compose up -d --build
```

⏳ Первый запуск может занять 5-10 минут (скачивание образов, сборка admin-panel).

### Шаг 3: Проверьте, что всё запустилось

```bash
docker-compose ps
```

Вы должны увидеть:
```
NAME                    STATUS
altshop                 Up
altshop-db              Up (healthy)
altshop-redis           Up (healthy)
altshop-nginx           Up
altshop-taskiq-worker   Up
altshop-taskiq-scheduler Up
admin-backend           Up (healthy)
admin-frontend          Up (healthy)
```

### 📊 Схема работы

```
┌─────────────────────────────────────────────────────────────┐
│                        СЕРВЕР                                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              altshop-nginx (порт 443)                 │   │
│  │                                                       │   │
│  │   /admin/*     → admin-frontend (порт 4080)          │   │
│  │   /admin/api/* → admin-backend (порт 4000)           │   │
│  │   /*           → altshop (порт 5000)                 │   │
│  └──────────────────────────────────────────────────────┘   │
│                           │                                  │
│           ┌───────────────┼───────────────┐                 │
│           ▼               ▼               ▼                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   altshop   │  │admin-frontend│  │admin-backend│         │
│  │  (бот)      │  │  (панель)   │  │   (API)     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│           │               │               │                 │
│           └───────────────┴───────────────┘                 │
│                           │                                  │
│                    remnawave-network                         │
│                    (Docker сеть)                             │
└─────────────────────────────────────────────────────────────┘
```

**Все контейнеры запускаются одной командой из папки `altshop`!**

---

## 👤 Создание администратора

Панель требует авторизации. Администратор создаётся автоматически при первом запуске на основе данных из `.env` файла.

### ✅ Автоматическое создание (рекомендуется)

Если вы правильно заполнили переменные в `.env`:
- `ADMIN_USERNAME` - логин администратора
- `ADMIN_PASSWORD` - пароль администратора
- `ADMIN_TELEGRAM_ID` - ваш Telegram ID

То администратор будет создан автоматически при первом запуске панели!

**Просто запустите панель и войдите с указанными данными.**

---

### 🔧 Ручное создание (если автоматическое не сработало)

Если нужно создать администратора вручную:

#### Шаг 1: Подключитесь к базе данных

```bash
docker exec -it altshop-db psql -U altshop -d altshop
```

#### Шаг 2: Создайте таблицу admin_users (если её нет)

Скопируйте и вставьте этот SQL-запрос:

```sql
CREATE TABLE IF NOT EXISTS admin_users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100),
    password VARCHAR(255) NOT NULL,
    telegram_id BIGINT UNIQUE,
    role VARCHAR(20) DEFAULT 'admin',
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

Нажмите Enter.

#### Шаг 3: Создайте администратора

⚠️ **ВАЖНО**: Пароль должен быть захеширован.

**Вариант 1: Простой пароль для теста (admin123)**
```sql
INSERT INTO admin_users (username, password, telegram_id, role, is_active)
VALUES (
    'admin',
    '$2b$10$N9qo8uLOickgx2ZMRZoMy.MqrqQlLBgXGlmPjPQQepHBqNDu1JXWS',
    123456789,
    'super_admin',
    true
);
```
Пароль: `admin123`

**Вариант 2: Пароль "password"**
```sql
INSERT INTO admin_users (username, password, telegram_id, role, is_active)
VALUES (
    'admin',
    '$2b$10$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW',
    123456789,
    'super_admin',
    true
);
```
Пароль: `password`

**⚠️ Не забудьте заменить `123456789` на ваш реальный Telegram ID!**

#### Шаг 4: Выйдите из базы данных

```bash
\q
```

### 🔐 Как создать свой пароль:

Если хотите свой пароль, используйте онлайн-генератор bcrypt хешей:
1. Откройте https://bcrypt-generator.com/
2. Введите желаемый пароль
3. Выберите "Rounds: 10"
4. Нажмите "Generate"
5. Скопируйте результат (начинается с `$2b$10$...`)
6. Вставьте в SQL-запрос вместо примера

### 🔄 Как сбросить пароль:

```bash
# Подключитесь к базе
docker exec -it altshop-db psql -U altshop -d altshop

# Обновите пароль (этот хеш = "newpassword123")
UPDATE admin_users
SET password = '$2b$10$N9qo8uLOickgx2ZMRZoMy.MqrqQlLBgXGlmPjPQQepHBqNDu1JXWS'
WHERE username = 'admin';

# Выйдите
\q
```

---

## ✅ Проверка работы

### Шаг 1: Откройте панель в браузере

Перейдите по адресу:
```
https://remnabot.2get.pro/admin
```

(Замените `remnabot.2get.pro` на ваш домен, если он другой)

### Шаг 2: Войдите в систему

Используйте данные из `.env` файла:
- **Username**: значение `ADMIN_USERNAME` (например: admin)
- **Пароль**: значение `ADMIN_PASSWORD` (который вы указали)

### Шаг 3: Смените пароль!

После первого входа обязательно смените пароль в настройках профиля.

---

## 🔧 Полезные команды

### Просмотр логов

```bash
# Все логи (из папки altshop)
docker-compose logs -f

# Только admin-backend
docker-compose logs -f admin-backend

# Только admin-frontend
docker-compose logs -f admin-frontend

# Только бот
docker-compose logs -f altshop
```

### Перезапуск только панели

```bash
cd altshop
docker-compose restart admin-backend admin-frontend
```

### Перезапуск всего

```bash
cd altshop
docker-compose restart
```

### Пересборка панели (после обновления кода)

```bash
cd altshop
docker-compose up -d --build admin-backend admin-frontend
```

### Обновление всего проекта

```bash
cd altshop
git pull  # если используете git
docker-compose up -d --build
```

---

## ❌ Решение проблем

### Проблема: Панель не открывается (502 Bad Gateway)

**Причина**: Контейнеры не запустились или nginx не видит их.

**Решение**:
```bash
# Проверьте статус контейнеров
cd admin_panel
docker-compose ps

# Если контейнеры не запущены, посмотрите логи
docker-compose logs

# Перезапустите
docker-compose down
docker-compose up -d --build
```

### Проблема: Ошибка подключения к базе данных

**Причина**: Неправильные данные в .env файле.

**Решение**:
```bash
# Проверьте данные в .env бота
cat ../altshop/.env | grep POSTGRES

# Убедитесь, что в admin_panel/.env такие же значения
cat .env | grep DATABASE
```

### Проблема: Не могу войти (неверный пароль)

**Причина**: Пароль не захеширован или неправильный хеш.

**Решение**:
```bash
# Подключитесь к базе
docker exec -it altshop-db psql -U altshop -d altshop

# Удалите старого пользователя
DELETE FROM admin_users WHERE username = 'admin';

# Создайте нового (см. раздел "Создание администратора")
```

### Проблема: Контейнеры не видят друг друга

**Причина**: Сеть remnawave-network не существует.

**Решение**:
```bash
# Создайте сеть
docker network create remnawave-network

# Перезапустите бота и панель
cd ../altshop
docker-compose down
docker-compose up -d

cd ../admin_panel
docker-compose down
docker-compose up -d
```

### Проблема: Порт 443 занят

**Причина**: Что-то уже использует порт 443.

**Решение**:
```bash
# Посмотрите, что использует порт
sudo lsof -i :443

# Обычно это nginx бота - это нормально
# Панель работает через тот же nginx
```

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что бот работает: `cd ../altshop && docker-compose ps`
3. Проверьте сеть: `docker network ls | grep remnawave`

---

## 📝 Краткая шпаргалка

```bash
# ===== УСТАНОВКА (выполнить один раз) =====

# 1. Загрузить файлы на сервер (git pull или SFTP)
cd /путь/к/проекту
git pull

# 2. Перейти в папку бота
cd altshop

# 3. Добавить JWT_SECRET в .env
nano .env
# Добавить в конец:
# JWT_SECRET=ваш_сгенерированный_ключ
# JWT_EXPIRES_IN=7d

# 4. Запустить всё (бот + панель)
docker-compose up -d --build

# ===== ГОТОВО! =====
# Открыть панель: https://remnabot.2get.pro/admin
```

### Если что-то пошло не так:

```bash
# Посмотреть логи панели
cd altshop
docker-compose logs -f admin-backend admin-frontend

# Посмотреть логи nginx
docker-compose logs -f altshop-nginx

# Проверить, что контейнеры запущены
docker-compose ps
```

---

**Готово! 🎉** Теперь у вас есть работающая админ-панель для управления ботом.

Панель доступна по адресу: **https://remnabot.2get.pro/admin**

> **Примечание:** Admin Panel запускается автоматически вместе с ботом. Никаких отдельных команд не требуется!